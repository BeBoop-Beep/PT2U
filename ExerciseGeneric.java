/*
ExerciseGeneric is an Interface that creates a method exerciseFile and implements generics for it's inherited class(es)
to use later.
*/
public interface ExerciseGeneric<E, T> {
    void exerciseFile(E painLocation, T pain);
}
